import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  counter: 1,
}

const mutations = {}

const getters = {}

export default new Vuex.Store({
  state,
  mutations,
  getters,
  actions: {},
})
